using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Script_Disable : MonoBehaviour
{
/*    public WebBrowser2D Web2d; // Drag your specific script component here in the Inspector

    void Update()
    {
        // Check for mouse click
        if (Input.GetMouseButtonDown(0))
        {
            // Perform raycast to check if the object is clicked
            Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);
            RaycastHit hit;

            if (Physics.Raycast(ray, out hit))
            {
                if (hit.collider.gameObject == this.gameObject)
                {
                    // Object is clicked, disable the specific script
                    DisableSpecificScript();
                }
            }
        }
    }

    void DisableSpecificScript()
    {
        // Disable the specific script component attached to this object
        if (Web2d != null)
        {
            Web2d.enabled = false;
        }
        else
        {
            Debug.LogWarning("Specific script not assigned!");
        }
    }*/
}
